local Keybind = "E" -- Put keybind here


local uis = game:GetService("UserInputService")
uis.InputBegan:Connect(function(key,gp)
    if key.KeyCode == Enum.KeyCode[Keybind] and gp == false then
    for i,v in pairs(game.Players:GetPlayers()) do
        for k = 1,5 do
local ohTable1 = {
    [1] = v,
    [2] = {
        ["charge_amm"] = 0,
        ["scope_shot"] = false
    },
    [3] = "GR338",
    [4] = false
}

workspace.GameMain.ApplyDamage:FireServer(ohTable1)
            end
        end
    end
end)

    game:GetService("RunService").RenderStepped:Connect(function()

local ohString1 = "sound"
local ohString2 = "Fire"

game:GetService("ReplicatedStorage").Events.ReplicateService:InvokeServer(ohString1, ohString2)


local ohString1 = "stain"
local ohVector32 = Vector3.new(50,50,50)
local ohVector34 = 1, 0, 0

game:GetService("ReplicatedStorage").Events.ReplicateService:InvokeServer(ohString1, ohVector32, ohVector34)

end)
